let initialState = {
    friends: [
        { id: 1, name: 'Mot' },
        { id: 2, name: 'Tic' },
        { id: 3, name: 'Dartonian' }
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}


export default sidebarReducer;